import {NextResponse} from "next/server";import Groq from "groq-sdk";import {z} from "zod";const schema=z.object({instruction:z.string().min(3).max(500)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({status:"needs_clarification",question:"What transfer should I prepare?"},{status:400});if(!process.env.GROQ_API_KEY)return NextResponse.json(localParse(parsed.data.instruction));const groq=new Groq({apiKey:process.env.GROQ_API_KEY});const completion=await groq.chat.completions.create({model:"llama-3.3-70b-versatile",temperature:0,messages:[{role:"system",content:"Extract a remittance draft. Never guess an ambiguous recipient. Return JSON with status, recipient, amount, purpose, schedule, question."},{role:"user",content:parsed.data.instruction}],response_format:{type:"json_object"}});return NextResponse.json(JSON.parse(completion.choices[0]?.message?.content||"{}"))}
function localParse(instruction:string){const amount=instruction.match(/\$([\d.]+)/)?.[1];const names=["maria","adi","lina"].filter(name=>instruction.toLowerCase().includes(name));if(!amount||names.length!==1)return{status:"needs_clarification",question:!amount?"How much should be sent?":"Which verified recipient do you mean?"};return{status:"ready",recipient:names[0],amount:Number(amount),purpose:instruction,schedule:instruction.toLowerCase().includes("next")?"future":"now"}}
