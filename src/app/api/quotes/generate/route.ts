import {NextResponse} from "next/server";import {z} from "zod";const schema=z.object({sender_id:z.string(),from_currency:z.string(),to_currency:z.string(),send_amount:z.number().positive().max(10000)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Invalid quote request"},{status:400});const rate=58.8;return NextResponse.json({quote_id:crypto.randomUUID(),rate,recipient_receives_amount:Number((parsed.data.send_amount*rate).toFixed(2)),locked_until:new Date(Date.now()+5*60_000).toISOString()})}
