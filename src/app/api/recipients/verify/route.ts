import {NextResponse} from "next/server";import {z} from "zod";const used=new Set<string>();const schema=z.object({verification_token:z.string().min(8).max(200)});export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Invalid verification token"},{status:400});const token=parsed.data.verification_token;if(used.has(token))return NextResponse.json({error:"Token already used"},{status:409});if(!token.startsWith("demo-")&&process.env.NODE_ENV!=="development")return NextResponse.json({error:"Token not recognized"},{status:404});used.add(token);return NextResponse.json({status:"verified"})}
