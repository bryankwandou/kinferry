import {NextResponse} from "next/server";import {z} from "zod";import {verifyToken} from "@/lib/tokens";const used=new Set<string>();const schema=z.object({verification_token:z.string().min(20).max(2000)});type Claims={type:"recipient_verification";jti:string;name:string;wallet:string;exp:number};
export async function POST(request:Request){const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Invalid verification token"},{status:400});const claims=verifyToken<Claims>(parsed.data.verification_token);if(!claims||claims.type!=="recipient_verification")return NextResponse.json({error:"Token is invalid or expired"},{status:404});if(used.has(claims.jti))return NextResponse.json({error:"Token already used"},{status:409});used.add(claims.jti);return NextResponse.json({status:"verified",recipient:{name:claims.name,wallet:claims.wallet}})}
