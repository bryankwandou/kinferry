import {NextResponse} from "next/server";import {executeGuarded,issueQuote} from "@/lib/execution";
export async function POST(request:Request){if(process.env.CRON_SECRET&&request.headers.get("authorization")!==`Bearer ${process.env.CRON_SECRET}`)return NextResponse.json({error:"Unauthorized"},{status:401});const quote=issueQuote(180,58.8);const check=executeGuarded({recipient_id:"maria",usdc_amount:180,request_nonce:`schedule-${Date.now()}`,raw_instruction:"monthly school support",quote_token:quote.quote_token});return NextResponse.json({processed:check.ok?1:0,blocked:check.ok?0:1,reason:check.reason})}
