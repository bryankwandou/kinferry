"use client";import {useState} from "react";import {Check,ShieldCheck} from "lucide-react";import {Logo} from "@/components/logo";export default function VerifyPage({params}:{params:Promise<{token:string}>}){const[done,setDone]=useState(false);async function verify(){const{token}=await params;const response=await fetch("/api/recipients/verify",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({verification_token:token})});setDone(response.ok)}return <main className="grid min-h-screen place-items-center bg-[#edf4f1] p-5"><section className="w-full max-w-lg rounded-3xl border border-[#d6e3de] bg-white p-8 shadow-xl"><Logo/><div className="mt-12 grid h-14 w-14 place-items-center rounded-2xl bg-[#e7f5ef] text-[#0f766e]">{done?<Check size={28}/>:<ShieldCheck size={28}/>}</div><h1 className="mt-6 text-3xl font-semibold tracking-tight">{done?"Wallet confirmed":"Confirm your receiving wallet"}</h1><p className="mt-3 leading-7 text-[#657570]">{done?"The sender can now add this address to their on-chain allowlist.":"This action confirms that you control the contact channel and recognize the wallet supplied for future transfers."}</p>{!done&&<><div className="mono mt-6 rounded-xl bg-[#f3f6f5] p-4 text-sm">9qvB...Z3nP</div><button onClick={verify} className="mt-6 w-full rounded-full bg-[#103d38] px-6 py-3 font-semibold text-white">I recognize this wallet</button></>}</section></main>}
