"use client";
import {AnimatePresence,motion,useReducedMotion} from "framer-motion";
import {LoaderCircle,LogOut,RefreshCw,Wallet} from "lucide-react";
import {useWallet} from "./wallet-context";
export function WalletControl(){const{address,balance,busy,error,connect,disconnect,refresh}=useWallet();const reduce=useReducedMotion();if(!address)return <div><button onClick={()=>connect()} disabled={busy} className="focus-ring inline-flex min-h-11 items-center gap-2 rounded-full bg-[#103d38] px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{busy?<LoaderCircle className="animate-spin" size={17}/>:<Wallet size={17}/>}Connect wallet</button>{error&&<span className="sr-only">{error}</span>}</div>;return <AnimatePresence mode="wait"><motion.div key={address} initial={reduce?false:{opacity:0,scale:.96}} animate={{opacity:1,scale:1}} className="flex items-center gap-2 rounded-full border border-[#cbdad5] bg-white py-1.5 pl-3 pr-1.5"><button onClick={()=>refresh()} aria-label="Refresh wallet balance" className="text-left"><span className="mono block text-[11px] font-semibold text-[#315b54]">{address.slice(0,4)}...{address.slice(-4)}</span><span className="block text-[10px] text-[#71817c]">{balance===null?"Loading":`${balance.toFixed(3)} devnet SOL`}</span></button><button onClick={()=>disconnect()} aria-label="Disconnect wallet" className="grid h-8 w-8 place-items-center rounded-full bg-[#f0f4f2] text-[#60706c]"><LogOut size={14}/></button><RefreshCw className="hidden"/></motion.div></AnimatePresence>}
